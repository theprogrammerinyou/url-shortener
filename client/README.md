# URL Shortener React Client

This is a simple React UI for the URL shortener backend.

## Run locally

1. Install dependencies
   ```bash
   cd client
   npm install
   ```
2. Start development server
   ```bash
   npm start
   ```

The app expects the backend to be available at `http://localhost:5000`.
